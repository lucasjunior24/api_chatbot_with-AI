
services:
  mongo:
    restart: always
    image: mongo:7.0.6
    container_name: mongo
    ports:
      - "27017:27017"
    networks:
      - notify-network
  

networks: 
  notify-network:
    name: notify-network
    driver: bridge