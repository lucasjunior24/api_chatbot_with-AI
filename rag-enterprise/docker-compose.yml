services:
  api:
    build: .
    ports:
      - "8000:8000"
    depends_on:
      - redis
      - ollama

  redis:
    image: redis:7

  ollama:
    image: ollama/ollama
    ports:
      - "11434:11434"